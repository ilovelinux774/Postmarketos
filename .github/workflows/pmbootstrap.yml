name: postmarketOS CI - samsung-kylepro

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y \
            git \
            python3 \
            python3-pip \
            python3-setuptools \
            python3-wheel \
            device-tree-compiler \
            cpio \
            curl \
            bzip2 \
            gzip \
            lzop \
            rsync \
            tar \
            wget \
            xz-utils \
            qemu-user-static \
            binfmt-support

          python3 -m pip install --upgrade pip setuptools wheel

      - name: Install pmbootstrap
        run: |
          git clone https://gitlab.postmarketos.org/postmarketOS/pmbootstrap.git
          cd pmbootstrap
          pip3 install --user -e .

          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Clone pmaports
        run: |
          git clone https://gitlab.postmarketos.org/postmarketOS/pmaports.git

      - name: Check versions
        run: |
          pmbootstrap --version
          cd pmaports && git log -1 --oneline

      - name: Init pmbootstrap
        env:
          PMB_DEVICE: samsung-kylepro
          PMB_CHANNEL: edge
        run: |
          printf "\n\n\n\n\n" | pmbootstrap init

      - name: Build kernel
        run: |
          pmbootstrap build linux-samsung-kylepro

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: kylepro-build
          path: /home/runner/.local/var/pmbootstrap/packages/
