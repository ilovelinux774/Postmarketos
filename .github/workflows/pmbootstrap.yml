name: postmarketOS CI - samsung-kylepro

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      # ----------------------------
      # System dependencies
      # ----------------------------
      - name: Install system dependencies
        run: |
          sudo apt update

          sudo apt install -y \
            git \
            python3 \
            python3-pip \
            python3-setuptools \
            python3-wheel \
            device-tree-compiler \
            cpio \
            curl \
            bzip2 \
            gzip \
            lzop \
            rsync \
            tar \
            wget \
            xz-utils \
            qemu-user-static \
            binfmt-support

          python3 -m pip install --upgrade pip setuptools wheel

      # ----------------------------
      # Install pmbootstrap from OFFICIAL git (no PyPI)
      # ----------------------------
      - name: Install pmbootstrap from git
        run: |
          git clone https://gitlab.com/postmarketOS/pmbootstrap.git
          cd pmbootstrap
          pip3 install --user -e .

          echo "$HOME/.local/bin" >> $GITHUB_PATH

      # ----------------------------
      # Clone pmaports (IMPORTANT: correct repo)
      # ----------------------------
      - name: Clone pmaports
        run: |
          git clone https://gitlab.com/postmarketOS/pmaports.git

      # ----------------------------
      # Verify versions (prevents "switched repos" errors)
      # ----------------------------
      - name: Check versions
        run: |
          ~/.local/bin/pmbootstrap --version
          cd pmaports && git log -1 --oneline

      # ----------------------------
      # Initialize pmbootstrap safely
      # ----------------------------
      - name: Init pmbootstrap
        run: |
          pmbootstrap --details-to-stdout \
            -p pmaports \
            init --non-interactive \
            --device samsung-kylepro \
            --channel edge

      # ----------------------------
      # Build kernel package
      # ----------------------------
      - name: Build kernel
        run: |
          pmbootstrap -p pmaports build linux-samsung-kylepro

      # ----------------------------
      # Upload artifacts
      # ----------------------------
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: kylepro-build
          path: /home/runner/.local/var/pmbootstrap/packages/
